apiVersion: v1
kind: Service
metadata:
  name: {{ include "tasksense.fullname" . }}
  labels: {{- include "tasksense.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector: {{- include "tasksense.selectorLabels" . | nindent 4 }}
